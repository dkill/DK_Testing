{
  "name": "dk_testing",
  "version": "1.0.0",
  "description": "an automated testing suite for the Dolls kill shopify storefront",
  "main": "index.js",
  "scripts": {
    "cy:run": "cypress run",
    "cy:run:nav": "cypress run --spec cypress/e2e/navigation",
    "cy:run:cart": "cypress run --spec cypress/e2e/addToCart",
    "cy:run:quick": "cypress run --spec cypress/e2e/addToCart/quickAdd.cy.js",
    "cy:run:search": "cypress run --spec cypress/e2e/search",
    "cy:run:footer": "cypress run --spec cypress/e2e/footer",
    "cy:run:wishlist": "cypress run --spec cypress/e2e/wishlist",
    "cy:run:minicart": "cypress run --spec cypress/e2e/miniCart"
  },
  "author": "Celina Oh",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^13.3.0",
    "cypress-if": "^1.10.5",
    "cypress-iframe": "^1.0.1",
    "cypress-wait-until": "^3.0.1",
    "dotenv": "^16.3.1"
  },
  "dependencies": {
    "cypress-commands": "^3.0.0",
    "cypress-plugin-steps": "^1.1.1",
    "cypress-real-events": "^1.11.0"
  }
}
